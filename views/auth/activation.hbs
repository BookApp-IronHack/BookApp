<section class="activation">
  <div class="belt">
    {{#if error}}
    <p class="activation-text">Tu cuenta no existe 😔. Por favor,
    <a href="/auth/signup">Registrarse</a></p>
    {{/if}}
    {{#if user}}
    <p class="activation-text">Tu cuenta ha sido activada con éxito 😊. Por favor,
    <a href="/auth/login">inicia sesión</a></p>
    {{/if}}
  </div>
</section>